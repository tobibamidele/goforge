package db

import (
	_ "github.com/lib/pq"
	"github.com/jmoiron/sqlx"

	"{{.Module}}/internal/config"
)

type DB struct {
	*sqlx.DB
}

func Connect(cfg *config.Config) (*DB, error) {
	sdb, err := sqlx.Connect("postgres", cfg.DatabaseURL)
	if err != nil {
		return nil, err
	}
	return &DB{sdb}, nil
}

func Close(d *DB) { _ = d.DB.Close() }
